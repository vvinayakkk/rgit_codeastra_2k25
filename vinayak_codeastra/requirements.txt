# Core Flask dependencies
Flask==3.0.3
flask-cors==4.0.0
# Twilio integration
twilio==9.0.5
# LangChain and related dependencies
langchain==0.2.0
langchain-google-genai==1.0.3
composio-langchain==0.1.3  # Note: Verify the exact package name/version as it might vary
# Document processing
python-docx==1.1.0
markdown==3.6
# Environment variable management
python-dotenv==1.0.1
# Additional dependencies that might be needed
requests==2.31.0  # Often required for API calls
google-auth==2.29.0  # For Google API authentication
pydantic==2.7.1  # Often used with LangChain